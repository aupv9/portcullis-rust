apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets (portcullis-mtls, portcullis-hmac) are created out-of-band by
# gen-dev-mtls.sh before `kubectl apply -k`, so they are intentionally NOT
# listed here.
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
